let nombre = prompt ("ingresa tu nombre ");
let apellido = prompt ("ingresa tu apellido");
let coma = ", " ;
let hola = "Hola Sr/a  ";
let mensaje = hola + nombre + coma + apellido ;

Alert (mensaje)
Alert ("Quieres Jugar??")

let juego = prompt ("SI o NO")

if (juego == SI || juego == Si || juego == si || juego == sI ){
    
    Alert ("Adivina el numero")
    Alert ("tenes 3 intentos")
   let elejido = prompt ("Ingresa un Numero")
    while (elejido !== 560){
        for (let i = 0; let= 3 ; i++){
            eljido = prompt (nombre "   Ingresa otro numero")
        }
    
    
    }    


        
    }
else {
    Alert ("Nos vemos,   " + nombre) 
}



/*if ()

let A  = prompt ("ingrese un numero")
let B  = prompt ("Ingrese otro numero")
let mult= (A * B)
let resmult= ("la multiplicacion es:   ")
let resultado = (resmult + mult)

alert (resultado)
*/
